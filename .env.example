# Configuração JWT (OBRIGATÓRIO em produção)
# IMPORTANTE: Copie este arquivo para .env e configure os valores reais
# NUNCA commite o arquivo .env com valores reais!

# Chave secreta JWT (mínimo 32 caracteres)
# Em produção, use uma chave forte gerada aleatoriamente
JWT_KEY=sua-chave-super-secreta-aqui-minimo-32-caracteres

# Emissor do token (issuer)
JWT_ISSUER=MinhaApi

# Audiência do token (audience)
JWT_AUDIENCE=MinhaApiClient

# Tempo de expiração do token em horas
JWT_EXPIRES_HOURS=2

# Chave secreta para Refresh Token (mínimo 32 caracteres)
JWT_REFRESH_KEY=sua-chave-refresh-token-super-secreta-aqui-minimo-32-caracteres

# Tempo de expiração do Refresh Token em dias
JWT_REFRESH_EXPIRES_DAYS=7
